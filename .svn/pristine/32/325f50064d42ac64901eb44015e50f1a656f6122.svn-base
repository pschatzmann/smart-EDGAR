package ch.pschatzmann.edgar.formatter;

/**
 * Interface which supports the reformatting of String values
 * @author pschatzmann
 *
 */
public interface IValueFormatter {
	String format(String value);
}
